services:
  web:
    build: ./academy-webapp
    ports:
      - "5173:5000"
    develop:
      watch:
        - action: sync
          path: .
          target: /code
  redis:
    image: "redis:alpine"